# Contact form: POSTs JSON { name, email, message } to this URL (Formspree, Make, Slack webhook, your API).
# Without it, messages are logged in development and rejected with 503 in production.
CONTACT_WEBHOOK_URL=

# Password for protected (NDA) case studies. Without it, protected projects stay locked.
PRIVATE_CASE_PASSWORD=

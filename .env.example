# Public URL of the deployed site (used for canonical/OG metadata).
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Where contact-form submissions are POSTed as JSON (Slack/Discord webhook, Zapier, email relay…).
# Leave unset and the form hands messages to the visitor's email app instead.
CONTACT_WEBHOOK_URL=
